PREREQUISITES:

A working maven-2.2 install
JDK 1.6
JCE installed


To build the framework simply call
mvn clean install

This command will run the testsuite and installs the jar in the local
maven-repository


To begin with the integration look at the WSSec class
,the SecurityOutInterceptor, the SecurityInInterceptor and
at the Test classes


"Marc Giger" <gigerstyle@gmx.ch>